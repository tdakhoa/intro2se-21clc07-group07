import React from "react";
import Layout from "../Layout";

const Playlists = () => {
    return (
        <Layout>
            <div>Playlists</div>
        </Layout>
    );
};

export default Playlists;
